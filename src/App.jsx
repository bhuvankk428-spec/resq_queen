import {useRef,useState} from 'react'; import {generateQuestions} from './lib/api'; import {getPlayer,savePlayer,recordResult} from './lib/storage';
import Splash from './components/SplashScreen'; import Home from './components/HomeScreen'; import Setup from './components/PlayerSetup'; import Story from './components/StoryIntro'; import Loading from './components/LoadingState'; import ErrorState from './components/ErrorState'; import Arena from './components/GameArena'; import QuestionCard from './components/QuestionCard'; import Result from './components/ResultScreen';
export default function App(){const stored=getPlayer(); const [view,setView]=useState('splash'),[player,setPlayer]=useState(stored),[name,setName]=useState(stored?.playerName||''),[topic,setTopic]=useState(''),[difficulty,setDifficulty]=useState('Mixed'),[questions,setQuestions]=useState([]),[current,setCurrent]=useState(0),[score,setScore]=useState(0),[lives,setLives]=useState(3),[selected,setSelected]=useState(null),[answered,setAnswered]=useState(false),[storyStep,setStoryStep]=useState(0),[error,setError]=useState(''); const requestId=useRef(0), aborter=useRef();
const start=()=>setView(player?'home':'setup'); const resetGame=()=>{setCurrent(0);setScore(0);setLives(3);setSelected(null);setAnswered(false)};
const launch=async()=>{if(!topic.trim()){setError('Please enter a topic before beginning your quest.');setView('error');return;} resetGame();setView('loading');const id=++requestId.current;aborter.current?.abort();aborter.current=new AbortController();try{const quest=await generateQuestions(topic,difficulty,aborter.current.signal);if(id!==requestId.current)return;setQuestions(quest.questions);setView('game')}catch(e){if(id!==requestId.current||e.name==='AbortError')return;setError(e.message||'A network error stopped the quest.');setView('error')}};
const setupDone=()=>{if(!name.trim()||!topic.trim()){setError('Please add both your name and a study topic.');setView('error');return;}const p={playerName:name.trim(),wins:0,losses:0};savePlayer(p);setPlayer(p);setStoryStep(0);setView('story')}; const answer=i=>{if(answered)return;setSelected(i);setAnswered(true);const correct=i===questions[current].correctAnswer;const nextScore=score+(correct?1:0),nextLives=lives-(correct?0:1);setScore(nextScore);setLives(nextLives);setTimeout(()=>{if(nextLives===0||current===9){const won=nextScore>=7;setPlayer(recordResult(player,won));setView('result')}else{setCurrent(c=>c+1);setSelected(null);setAnswered(false)}},1150)};
if(view==='splash')return <Splash start={start}/>;if(view==='setup')return <Setup {...{name,setName,topic,setTopic,difficulty,setDifficulty}} continueToStory={setupDone}/>;if(view==='story')return <Story step={storyStep} next={()=>storyStep===4?launch():setStoryStep(s=>s+1)}/>;if(view==='loading')return <Loading/>;if(view==='error')return <ErrorState error={error} retry={launch} home={()=>setView(player?'home':'setup')}/>;if(view==='home')return <Home {...{player,topic,setTopic,difficulty,setDifficulty}} begin={launch} newPlayer={()=>{localStorage.removeItem('save-the-queen-player');setPlayer(null);setName('');setView('setup')}}/>;if(view==='result')return <Result won={score>=7} {...{score,lives}} again={launch} home={()=>setView('home')}/>;return <main className="game"><Arena score={score}/><QuestionCard question={questions[current]} index={current} {...{score,lives,selected,answered}} onAnswer={answer}/></main>}
